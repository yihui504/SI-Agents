{
  "name": "@si-agents/core",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "si-agents": "./bin/si-agents.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "cli": "bun run bin/si-agents.ts",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "hono": "^4.6.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
